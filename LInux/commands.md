# 🛠️ Linux Basic Commands

Podstawowe operacje na plikach i nawigacja po systemie.

---

## 🏗️ Nawigacja i System

- `pwd` – wyświetla pełną ścieżkę do bieżącego katalogu.
- `ls -lah` – lista plików z uprawnieniami, ukrytymi plikami i czytelnym rozmiarem.
- `cd -` – powrót do poprzednio odwiedzonego katalogu.
- `mkdir -p path/to/dir` – tworzy folder wraz z brakującymi folderami nadrzędnymi.

## 📂 Zarządzanie Plikami

- `cp -r <src> <dest>` – kopiowanie folderów wraz z zawartością.
- `mv <old> <new>` – przenoszenie lub zmiana nazwy pliku/folderu.
- `rm -rf <name>` – rekurencyjne i wymuszone usuwanie (używaj ostrożnie!).
- `touch <file>` – szybkie tworzenie pustego pliku.

## 💡 Productivity Hacks

- `Ctrl + R` – przeszukiwanie historii komend.
- `alias ll="ls -lah"` – skrócenie najczęstszej komendy listowania.
- `tldr <cmd>` – uproszczona dokumentacja zamiast nudnego `man`.
